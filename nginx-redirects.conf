# Old WordPress URL redirects — preserve link equity from Google's index
# Include this in the server block for telephony.museum

# /en/home/ → /
location = /en/home/ {
    return 301 /;
}

# /en/exposition/ → /exposition/
location = /en/exposition/ {
    return 301 /exposition/;
}

# /en/education/ → /education/
location = /en/education/ {
    return 301 /education/;
}

# /en/prices/ → /prices/
location = /en/prices/ {
    return 301 /prices/;
}

# /en/contacts/ → /contacts/
location = /en/contacts/ {
    return 301 /contacts/;
}

# /parama now served by the Astro site — redirect removed
